# packages/config

Shared base configs (tsconfig, eslint, prettier) consumed by apps/api and apps/admin. Populated as part of the Backend Foundation and Admin Foundation chunks so both apps start from the same lint/format baseline.

# core/providers

App-wide Riverpod providers (feature flags, auth state, connectivity, etc.) land here as their backing modules are approved. Empty as of Flutter Foundation — splash doesn't need any provider beyond the router itself.

# packages/shared

Cross-app TypeScript types (API DTOs, enums) shared between apps/api and apps/admin. Empty until apps/api's OpenAPI spec exists to generate from (Sprint 1 backend foundation chunk).
